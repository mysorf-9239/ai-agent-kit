# Reports

This directory stores human-readable summaries derived from structured outputs.

Rules:
- reports MUST be derived from machine-checkable outputs
- reports MUST remain grouped by `run_id`
- reports MUST NOT be the only source of truth
