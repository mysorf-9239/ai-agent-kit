# Artifacts

This directory stores machine-generated artifacts grouped by `run_id`.

Rules:
- artifacts MUST be machine-checkable
- artifacts MUST align with `templates/output-schemas/`
- artifacts MUST be referenced by state
