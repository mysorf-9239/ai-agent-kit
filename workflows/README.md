# Workflows

This directory stores multi-skill execution pipelines.

Current baseline workflow:
- `dti-benchmark-training`
- `dti-evaluation-only`
- `dti-run-comparison`

Rules:
- workflows MUST compose named skills only
- branch logic MUST be explicit
- completion MUST be testable
